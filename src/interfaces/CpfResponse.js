/**
 * @typedef {Object} CpfResponse
 * @property {boolean} isValid - Indica se o CPF é válido
 * @property {string} message - Mensagem de validação
 */

export const exampleCpfResponse = {
  isValid: true,
  message: 'CPF válido'
}
